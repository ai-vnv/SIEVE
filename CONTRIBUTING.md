# Development

This private research artifact is maintained by **mansurarief**. Use a branch for changes, run `npm test` and `npm run experiment`, and preserve seed sets, input provenance, units, and model limitations. Regenerate evidence after changing a dependency recorded in `results/assurance-case.json`.

Do not replace synthetic labels with measured labels without traceable source records. Keep illustrative animations separate from numerical claims. New result annotations must read experiment outputs rather than copying numbers by hand.
